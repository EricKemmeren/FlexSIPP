[build-system]
requires = ["meson", "meson-python", "pybind11"]
build-backend = "mesonpy"

[project]
name = "FlexSIPP"
version = "0.0.1"
dependencies = [
    "numpy>=1.25.1",
    "matplotlib>=3.10.8",
    "sortedcontainers>=2.4.0",
    "tqdm>=4.67.3",
]